{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "if [ -f backend/package-lock.json ]; then npm --prefix backend ci; else npm ci; fi"
  },
  "deploy": {
    "startCommand": "if [ -f backend/package.json ]; then npm --prefix backend start; else npm start; fi",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
